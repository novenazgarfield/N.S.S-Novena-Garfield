# 🐳 Changlee音乐播放器 - 后端服务 Dockerfile
FROM node:18-alpine

LABEL maintainer="N.S.S-Novena-Garfield Team"
LABEL description="Changlee音乐播放器 - 后端服务"

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apk add --no-cache curl ffmpeg

# 复制package文件
COPY package*.json ./

# 安装Node.js依赖
RUN npm ci --only=production

# 复制应用代码
COPY . .

# 创建必要目录
RUN mkdir -p data logs music

# 设置环境变量
ENV NODE_ENV=production
ENV BACKEND_PORT=8083

# 暴露端口
EXPOSE 8083

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8083/health || exit 1

# 启动命令
CMD ["node", "easy_start.js", "--backend-only"]